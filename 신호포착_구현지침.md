# 신호포착 시스템 구현지침 (Ver 3.0)

**문서 버전**: 3.0.0  
**최종 업데이트**: 2026-01-03  
**담당 시스템**: 실시간 신호 포착 & 알림 내역

---

## 📋 목차

1. [시스템 개요](#시스템-개요)
2. [신호 발생 히스토리](#신호-발생-히스토리)
3. [문자 발송 시스템](#문자-발송-시스템)
4. [데이터베이스 구조](#데이터베이스-구조)
5. [UI 컴포넌트](#ui-컴포넌트)
6. [API 엔드포인트](#api-엔드포인트)

---

## 시스템 개요

### 핵심 기능
신호포착 시스템은 MASTER CONTROL TOWER에서 발생한 매수/매도 신호를 기록하고, SMS 알림을 발송하는 시스템입니다.

### 주요 구성요소
1. **신호 발생 히스토리**: 자동 탐지된 매수/매도 신호 기록
2. **문자 발송 히스토리**: SMS 발송 내역 및 상태
3. **SMS 전체 가동**: 문자 발송 ON/OFF 제어

### 데이터 흐름
```
MASTER CONTROL TOWER (진입조건 완성)
    ↓
DB 저장 (signal_history)
    ↓
SMS 전체 가동 ON?
    ↓ YES
SMS 발송 + 발송 히스토리 저장 (sms_logs)
    ↓ NO
저장만 (발송 안 함)
```

---

## 신호 발생 히스토리

### 저장 조건

#### 1. 진입조건 완성 시 (처음 1번만)
```python
# backend/analysis.py (Line 1451-1492)
if result["step1"] and result["step2"] and result["step3"]:
    result["final"] = True
    if not state.get("final_met"):  # ← 처음 1번만
        state["final_met"] = True
        
        # DB 저장
        save_signal({
            'ticker': ticker,
            'signal_type': "BUY (MASTER)",
            'signal_reason': "진입조건 완성 (30분추세+박스돌파+5분타이밍)",
            'time_kst': time_kst_formatted,
            'time_ny': time_ny_formatted,
            ...
        })
```

#### 2. 중복 방지
```python
# 30분 이내 동일 종목 BUY 신호 확인
cursor.execute("""
    SELECT id FROM signal_history 
    WHERE ticker=%s AND signal_type='BUY (MASTER)' 
    AND created_at >= NOW() - INTERVAL 30 MINUTE 
    LIMIT 1
""", (ticker,))

if not cursor.fetchone():  # ← 없으면 저장
    save_signal({...})
```

#### 3. State 초기화 (조건 깨짐 시)
```python
else:
    result["final"] = False
    if state.get("final_met"):
        state["final_met"] = False  # ← 초기화
        state["signal_time"] = None
```

### 표시 형식

#### 테이블 컬럼
| 컬럼 | 너비 | 정렬 | 설명 |
|------|------|------|------|
| 발생 시간 | 160px | left | yyyy.MM.dd HH:mm (초 제거) |
| 종목 | 90px | left | SOXL, SOXS, UPRO |
| 구분 | 90px | center | 매수/매도/경보 (한글) |
| 신호 이유 | auto | left | signal_reason 또는 interpretation |
| 신호가 | 110px | right | $XX.XX (소수점 2자리) |
| 현재가 | 110px | right | $XX.XX (MASTER CONTROL TOWER에서 가져옴) |
| 수익률 | 100px | center | +X.XX% (계산) |
| 관리 | 80px | center | 삭제 버튼 |

#### 시간 형식
```javascript
// Ver 3.0: yyyy.MM.dd HH:mm (초 제거)
const year = date.getFullYear();
const month = String(date.getMonth() + 1).padStart(2, '0');
const day = String(date.getDate()).padStart(2, '0');
const hours = String(date.getHours()).padStart(2, '0');
const minutes = String(date.getMinutes()).padStart(2, '0');
kstTime = `${year}.${month}.${day} ${hours}:${minutes}`;

// 예시: 2026.01.03 16:30
```

#### 구분 표시
```javascript
// BUY/SELL 판단
const isBuy = signalType.includes('BUY');
const isSell = signalType.includes('SELL');
const isWarning = signalType.includes('WARNING');

if (isBuy) {
    actionText = '매수';
    actionColor = '#ef4444';  // 붉은색
    bgColor = 'rgba(239, 68, 68, 0.15)';
} else if (isSell) {
    actionText = '매도';
    actionColor = '#3b82f6';  // 파란색
    bgColor = 'rgba(59, 130, 246, 0.15)';
} else if (isWarning) {
    actionText = '경보';
    actionColor = '#eab308';  // 노란색
    bgColor = 'rgba(234, 179, 8, 0.15)';
}
```

#### 현재가 가져오기
```javascript
// Ver 3.0: MASTER CONTROL TOWER에서 현재가 가져오기
if (reportData.market_regime && reportData.market_regime.details) {
    const details = reportData.market_regime.details;
    
    priceMap['SOXL'] = details.soxl.current_price;
    priceMap['SOXS'] = details.soxs.current_price;
    priceMap['UPRO'] = details.upro.current_price;
}
```

#### 수익률 계산
```javascript
const calcProfit = (signalPrice, currentPrice, type) => {
    if (!signalPrice || !currentPrice) return '-';
    let pct = 0;
    if (type === 'BUY') {
        pct = ((currentPrice - signalPrice) / signalPrice) * 100;
    } else {
        // For SELL signal (Short view)
        pct = ((signalPrice - currentPrice) / signalPrice) * 100;
    }
    return `${pct >= 0 ? '+' : ''}${pct.toFixed(2)}%`;
};
```

---

## 문자 발송 시스템

### SMS 전체 가동

#### ON/OFF 제어
```javascript
// Frontend: SignalPage.jsx
const toggleSms = async (enabled) => {
    const res = await fetch('/api/settings/sms', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ enabled })
    });
    if (res.ok) {
        const data = await res.json();
        setSmsEnabled(data.enabled);
    }
};
```

#### Backend 설정
```python
# backend/main.py
@app.post("/api/settings/sms")
def api_set_sms_setting(request: dict):
    enabled = request.get("enabled", True)
    set_global_config("sms_enabled", enabled)
    return {"enabled": enabled}
```

### 문자 발송 조건

#### 1. SMS 전체 가동 ON
```python
sms_enabled = get_global_config("sms_enabled", True)
if not sms_enabled:
    return  # 발송 안 함
```

#### 2. 신호 발생
- 진입조건 완성 (BUY)
- 경보 발생 (WARNING)
- 청산 신호 (SELL)

#### 3. 중복 방지
```python
# 30분 이내 동일 종목/신호 타입 확인
recent_sms = check_recent_sms_log(stock_name, signal_type, timeframe_minutes=30)
if recent_sms:
    return  # 발송 안 함
```

### 문자 발송 히스토리

#### 표시 제한
```javascript
// Ver 3.0: 최근 20개만 표시
const res = await fetch('/api/sms/history?limit=20');
```

#### Backend API
```python
@app.get("/api/sms/history")
def api_get_sms_history(limit: int = 30):
    return get_sms_logs(limit=limit)
```

#### 테이블 컬럼
| 컬럼 | 너비 | 정렬 | 설명 |
|------|------|------|------|
| 전송 일시 | 150px | left | MM.dd HH:mm |
| 수신 번호 | 130px | left | 010-XXXX-XXXX |
| 메시지 내용 | auto | left | SMS 본문 |
| 관리 | 120px | center | OK 상태 + 삭제 버튼 |

---

## 데이터베이스 구조

### signal_history 테이블

```sql
CREATE TABLE signal_history (
    id INT AUTO_INCREMENT PRIMARY KEY,
    ticker VARCHAR(10) NOT NULL,
    name VARCHAR(100),
    signal_type VARCHAR(50),           -- 'BUY (MASTER)', 'SELL', 'WARNING'
    signal_reason VARCHAR(100),        -- Ver 3.0: 신호 발생 이유
    position_desc VARCHAR(255),
    price DECIMAL(10, 2),
    signal_time DATETIME,
    time_kst VARCHAR(50),              -- Ver 3.0: 한국시간
    time_ny VARCHAR(50),               -- Ver 3.0: 미국시간
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    is_sent BOOLEAN DEFAULT FALSE,
    score INT,
    interpretation VARCHAR(255)
);
```

### sms_logs 테이블

```sql
CREATE TABLE sms_logs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    receiver VARCHAR(20),
    message TEXT,
    status VARCHAR(20),                -- 'Success', 'Failed'
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### global_config 테이블

```sql
CREATE TABLE global_config (
    key_name VARCHAR(50) PRIMARY KEY,
    value_json JSON,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
);

-- SMS 설정
INSERT INTO global_config (key_name, value_json) 
VALUES ('sms_enabled', 'true');
```

---

## UI 컴포넌트

### 필터 섹션

```javascript
<form onSubmit={applyFilters}>
    <div className="form-group">
        <label>시작일</label>
        <input type="date" name="start_date" value={filters.start_date} />
    </div>
    <div className="form-group">
        <label>종료일</label>
        <input type="date" name="end_date" value={filters.end_date} />
    </div>
    <div className="form-group">
        <label>표시 개수</label>
        <select name="limit" value={filters.limit}>
            <option value="30">30개</option>
            <option value="50">50개</option>
            <option value="100">100개</option>
        </select>
    </div>
    <button type="submit">조회</button>
    <button type="button" onClick={sendSampleSms}>테스트</button>
</form>
```

### SMS 전체 가동 제어

```javascript
<div style={{ display: 'flex', alignItems: 'center', gap: '1rem' }}>
    <span>SMS 전체 가동:</span>
    <label>
        <input type="radio" checked={smsEnabled} onChange={() => toggleSms(true)} />
        ON
    </label>
    <label>
        <input type="radio" checked={!smsEnabled} onChange={() => toggleSms(false)} />
        OFF
    </label>
</div>
```

### 신호 발생 히스토리 테이블

```javascript
<table style={{ minWidth: '1000px' }}>
    <thead>
        <tr>
            <th style={{ width: '160px' }}>발생 시간</th>
            <th style={{ width: '90px' }}>종목</th>
            <th style={{ width: '90px' }}>구분</th>
            <th>신호 이유</th>
            <th style={{ width: '110px' }}>신호가</th>
            <th style={{ width: '110px' }}>현재가</th>
            <th style={{ width: '100px' }}>수익률</th>
            <th style={{ width: '80px' }}>관리</th>
        </tr>
    </thead>
    <tbody>
        {signals.map(sig => (
            <tr key={sig.id}>
                <td>{kstTime}</td>
                <td>{sig.ticker}</td>
                <td>
                    <span style={{ 
                        minWidth: '50px', 
                        background: bgColor, 
                        color: actionColor 
                    }}>
                        {actionText}
                    </span>
                </td>
                <td>{sig.signal_reason || sig.interpretation}</td>
                <td>${Number(sig.price).toFixed(2)}</td>
                <td>${Number(prices[sig.ticker]).toFixed(2)}</td>
                <td>{calcProfit(sig.price, prices[sig.ticker], type)}</td>
                <td><button onClick={() => deleteSignal(sig.id)}>삭제</button></td>
            </tr>
        ))}
    </tbody>
</table>
```

---

## API 엔드포인트

### 신호 관련

#### GET /api/signals
신호 히스토리 조회

**파라미터**:
- `start_date`: 시작일 (YYYY-MM-DD)
- `end_date`: 종료일 (YYYY-MM-DD)
- `limit`: 표시 개수 (기본 30)

**응답**:
```json
[
    {
        "id": 1,
        "ticker": "SOXL",
        "signal_type": "BUY (MASTER)",
        "signal_reason": "진입조건 완성 (30분추세+박스돌파+5분타이밍)",
        "price": 46.50,
        "signal_time": "2026-01-03T07:30:00",
        "time_kst": "2026-01-03 16:30",
        "time_ny": "2026-01-03 02:30",
        "score": 100,
        "interpretation": "마스터 트리플 필터 진입"
    }
]
```

#### DELETE /api/signals/{id}
특정 신호 삭제

#### DELETE /api/signals/all
모든 신호 삭제

### SMS 관련

#### GET /api/sms/history
SMS 발송 히스토리 조회

**파라미터**:
- `limit`: 표시 개수 (기본 30, Ver 3.0에서는 20)

**응답**:
```json
[
    {
        "id": 1,
        "receiver": "010-1234-5678",
        "message": "[청안] SOXL 매수 진입 신호 발생...",
        "status": "Success",
        "created_at": "2026-01-03T16:30:00"
    }
]
```

#### POST /api/sms/test
테스트 문자 발송

**요청**:
```json
{
    "stock_name": "SOXL (Sample)",
    "signal_type": "매수 진입",
    "price": 45.20,
    "reason": "수동 테스트 발송"
}
```

#### DELETE /api/sms/history/{id}
특정 SMS 로그 삭제

#### DELETE /api/sms/history/all
모든 SMS 로그 삭제

### 설정 관련

#### GET /api/settings/sms
SMS 전체 가동 상태 조회

**응답**:
```json
{
    "enabled": true
}
```

#### POST /api/settings/sms
SMS 전체 가동 ON/OFF

**요청**:
```json
{
    "enabled": true
}
```

---

## 주요 로직

### 신호 저장 흐름

```
1. MASTER CONTROL TOWER에서 진입조건 완성 감지
   ↓
2. state["final_met"] 확인
   - False: 처음 진입 → 3번으로
   - True: 이미 저장됨 → 종료
   ↓
3. DB에서 30분 이내 동일 신호 확인
   - 없음: 저장 → 4번으로
   - 있음: 중복 → 종료
   ↓
4. signal_history 테이블에 저장
   - ticker, signal_type, signal_reason
   - time_kst, time_ny
   - price, score, interpretation
   ↓
5. SMS 전체 가동 확인
   - ON: SMS 발송 + sms_logs 저장
   - OFF: 저장만 (발송 안 함)
```

### 조건 초기화 흐름

```
1. 실시간 분석 중 조건 확인
   ↓
2. Step1, Step2, Step3 중 하나라도 False?
   - Yes: 3번으로
   - No: 진입조건 유지
   ↓
3. state["final_met"] 초기화
   - state["final_met"] = False
   - state["signal_time"] = None
   ↓
4. 다음 분석 시 새로운 진입으로 인식
```

---

## 문제 해결

### 현재가가 표시되지 않을 때

**원인**: MASTER CONTROL TOWER 데이터 구조 변경

**해결**:
```javascript
// Ver 3.0: market_regime.details에서 가져오기
if (reportData.market_regime && reportData.market_regime.details) {
    const details = reportData.market_regime.details;
    priceMap['SOXL'] = details.soxl.current_price;
    priceMap['SOXS'] = details.soxs.current_price;
    priceMap['UPRO'] = details.upro.current_price;
}
```

### 매수/매도 구분이 잘못 표시될 때

**원인**: signal_type이 'BUY (MASTER)' 형식인데 'BUY'로만 비교

**해결**:
```javascript
// includes() 사용
const isBuy = signalType.includes('BUY');
const isSell = signalType.includes('SELL');
```

### 중복 신호가 저장될 때

**원인**: state 초기화 또는 DB 중복 체크 실패

**해결**:
1. state["final_met"] 확인
2. DB에서 30분 이내 동일 신호 확인
3. 두 가지 모두 통과해야 저장

---

## 버전 히스토리

### Ver 3.0.0 (2026-01-03)
- **신호 이유 추가**: signal_reason 컬럼 추가
- **시간 시스템**: time_kst, time_ny 이중 저장
- **한글 표시**: BUY → 매수, SELL → 매도
- **현재가 소스**: MASTER CONTROL TOWER에서 가져오기
- **SMS 제한**: 문자발송 히스토리 20개로 제한
- **시간 형식**: yyyy.MM.dd HH:mm (초 제거)

### Ver 2.x
- 기본 신호 저장 및 SMS 발송 기능
- 다중 종목 지원

---

**이 문서는 신호포착 시스템의 완전한 구현 가이드입니다. 시스템 유지보수 및 개발 시 참고하세요.**
