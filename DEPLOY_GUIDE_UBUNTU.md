# 🚀 Ubuntu Deployment Guide (Ver 5.0+)

이 문서는 Cheongan Fintech 프로젝트(React Frontend + FastAPI Backend)를 Ubuntu 환경에 배포하는 방법을 설명합니다.
**Ver 5.0 이상**부터는 `manual_deploy.sh` 스크립트를 통해 "One-Click 배포"를 지원합니다.

---

## 1. 자동 배포 (권장)

가장 쉽고 안전한 방법입니다. 소스 코드를 수정한 후 아래 스크립트만 실행하세요.

```bash
./manual_deploy.sh
```

### 이 스크립트가 하는 일:
1.  **Frontend 빌드**: `frontend/` 폴더에서 `npm run build`를 실행하여 최적화된 정적 파일(`dist/`)을 생성합니다.
2.  **파일 복사**: 생성된 `dist` 파일들을 웹 서버 루트(`/var/www/html`)로 자동 복사합니다. (sudo 권한 사용)
3.  **Backend 재시작**: `cheongan-backend` 서비스를 재시작하여 Python 코드 변경 사항을 반영합니다.

> **주의**: 스크립트 실행 중 `sudo` 비밀번호 입력을 요청할 수 있습니다.

---

## 2. 수동 배포 (문제 발생 시)

스크립트가 작동하지 않거나 개별적인 제어가 필요할 때 사용합니다.

### A. Frontend 배포 (화면 업데이트)

```bash
# 1. 빌드
cd /home/blue/blue/my_project/money/frontend
npm run build

# 2. 결과물 확인
ls -l dist/

# 3. 웹 서버로 복사 (Nginx 등)
sudo cp -r dist/* /var/www/html/
```

### B. Backend 배포 (로직 업데이트)

```bash
# 1. 서비스 재시작
sudo systemctl restart cheongan-backend

# 2. 상태 확인
sudo systemctl status cheongan-backend
```

### C. 로그 확인

```bash
# 실시간 로그 모니터링
journalctl -u cheongan-backend -f
```

---

## 3. 트러블슈팅

### Q1. "업데이트 실패" 또는 화면이 그대로예요.
*   **원인**: 브라우저 캐시 때문일 가능성이 90%입니다.
*   **해결**: `Ctrl + Shift + R` (강제 새로고침)을 하세요.
*   **확인**: 그래도 안 되면 `manual_deploy.sh`를 다시 실행하여 `/var/www/html/index.html` 의 생성 시간이 현재인지 확인하세요.

### Q2. "Backend Restart Failed" 오류가 떠요.
*   `sudo` 비밀번호 정합성을 확인하세요.
*   Python 코드에 치명적인 문법 오류(Syntax Error)가 있으면 서비스가 뜨지 않습니다. `journalctl` 명령어로 로그를 확인하세요.

---

## 4. 환경 변수 및 경로

*   **프로젝트 루트**: `/home/blue/blue/my_project/money`
*   **Frontend 소스**: `frontend/`
*   **Backend 소스**: `backend/`
*   **Web Server Root**: `/var/www/html` (Nginx Default)
*   **Service Name**: `cheongan-backend`
